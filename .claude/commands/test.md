---
description: Run tests and report results
allowed-tools: Bash(pnpm test*), Bash(npx *)
model: haiku
---
Run `pnpm test` and summarize: how many passed, how many failed, which ones failed and why. Be brief.
